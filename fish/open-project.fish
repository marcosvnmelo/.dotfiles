complete -c op -f

# Add description for the base command
complete -c op -d "Open projects in tmux sessions"

# Add completion for --preset flag
complete -c op -l preset -d "Open projects in tmux sessions"
